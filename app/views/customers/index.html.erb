<article ng-app="customers" ng-controller="CustomerSearchController">
	<header>
		<h1 class="h2">Customer Search</h1>
	</header>
	
	<section class="search-form">
		<form>
			<label for="keywords" class="sr-only">Keywords</label>
			<input class="form-control input-lg" name="keywords" 
																					 type="text" 
																					 placeholder="First Name, Last Name or Email Address" 
																					 ng-model="keywords"
																					 ng-change="search(keywords)" >
		</form>
	</section>

	<section class="search-results">
		<header>
			<h1 class="h3">Results</h1>
		</header>
		<%= render partial: "pager", locals: {keywords: params[:keywords], page: @page} %>
		<ol class="list-group">
			<li class="list-group-item clearfix" ng-repeat="customer in customers">
				<h3 class="pull-right">
					<small class="text-uppercase">Joined</small> 
					{{ customer.created_at | date }}
				</h3>
				<h2 class="h3">
					{{ customer.first_name }} {{ customer.last_name }}
					<small>{{ customer.username }}</small>
				</h2>
				<h4>{{ customer.email }}</h4>
			</li>
		</ol>
		<%= render partial: "pager", locals: {keywords: params[:keywords], page: @page} %>
	</section>
</article>
